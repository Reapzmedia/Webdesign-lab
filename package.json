{
  "name": "apex-base-project",
  "version": "1.0.0",
  "description": "Modern HTML5 base project with design tokens, Live Server, Playwright, and static audit tools.",
  "scripts": {
    "playwright": "playwright",
    "test": "playwright test",
    "codegen": "playwright codegen http://localhost:5500",
    "audit": "powershell -ExecutionPolicy Bypass -File .agents/skills/skylos-audit/scripts/audit.ps1"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "playwright": "^1.50.0"
  }
}
